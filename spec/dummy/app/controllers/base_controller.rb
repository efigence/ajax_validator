# if the engine inherits from host application's controller,
# for example SomeEngineController < ::BaseController,
# class must exist in dummy app in order for routing tests to recognize it
class BaseController
end